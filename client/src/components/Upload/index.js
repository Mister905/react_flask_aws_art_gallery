import React from "react";
import { Link } from "react-router-dom";

const Upload = () => {
  return (
    <div>
      <h1>Upload</h1>

      <Link to={'/'}>Gallery</Link>
    </div>
  );
};

export default Upload;
